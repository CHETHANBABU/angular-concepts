import { CIfDirective } from './c-if.directive';

describe('CIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CIfDirective();
    expect(directive).toBeTruthy();
  });
});
