import { CForDirective } from './c-for.directive';

describe('CForDirective', () => {
  it('should create an instance', () => {
    const directive = new CForDirective();
    expect(directive).toBeTruthy();
  });
});
