import { CDelayDirective } from './c-delay.directive';

describe('CDelayDirective', () => {
  it('should create an instance', () => {
    const directive = new CDelayDirective();
    expect(directive).toBeTruthy();
  });
});
