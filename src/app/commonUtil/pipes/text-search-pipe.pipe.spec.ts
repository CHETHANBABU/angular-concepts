import { TextSearchPipePipe } from './text-search-pipe.pipe';

describe('TextSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TextSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
